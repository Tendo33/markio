# Review Checklist

- [ ] API, SDK/CLI, Vue console, docs, and tests agree.
- [ ] `/v1/*` auth remains enforced.
- [ ] Task owner/admin boundaries remain intact.
- [ ] Parser changes preserve request validation and error behavior.
- [ ] Redis remains optional unless explicitly scoped otherwise.
- [ ] Frontend still builds to `markio/webapp` with base `/console/`.
- [ ] Verification commands from `shared/verification.md` were run or explicitly skipped with a reason.
